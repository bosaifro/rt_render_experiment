package dev.rt_render_experiment.reentry;

import dev.rt_render_experiment.integration.minecraft.MinecraftSectionSemantics;
public interface SemanticMesh {
    MinecraftSectionSemantics.Metadata rt_render_experiment$semantics();
    void rt_render_experiment$semantics(MinecraftSectionSemantics.Metadata metadata);
}
